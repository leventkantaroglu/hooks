import 'package:flutter/material.dart';

// TODO apa c

class App extends StatelessWidget {
  final bool isVersionValid;
  const App({required this.isVersionValid, super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
